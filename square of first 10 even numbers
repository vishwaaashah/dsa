#include <stdio.h>

int main()
{
    int i;

    printf("Squares of the first 10 even numbers are:\n");

    for (i = 2; i <= 20; i = i + 2)
    {
        printf("%d ", i * i);
    }

    return 0;
}
