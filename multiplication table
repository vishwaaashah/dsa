#include <stdio.h>

int main()
{
	int num, i;
	printf("enter any number for the multiplication table :");
	scanf_s("%d", &num);
	for (i = 1; i < 11; i++)
	{
		printf("%d * %d = %d\n", num, i, num * i);
	}
	return 0;
}
